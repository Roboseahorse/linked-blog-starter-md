# Homework 2: [[Thinking Ahead - 15-09-26|Thinking Ahead]]

1. (a) Using the headings given, specify the inputs, outputs and precondition for the following problem:

   > Given a list of integers, return a list containing every number divisible by 7. [4]

   **Name:** divisibleBy7
   **Inputs:** list of integers
   **Outputs:** List of integers
   **Precondition:** The list is not empty

   (b) Write a pseudocode algorithm for this function, and a main program which calls the function and outputs the resulting list. [4]

   ```
## function
def divsibleBy7 (list):
	length=len(list)
	for i in range length:
		if (list[i]) MOD 7 == 0:
			newlist.append(list[i])
	return newlist
## main program
numbers = [0 2 4 324 45 6 7 5 21 27 9 4]
print(divisibleBy7(numbers))
   ```

   (c) Give **two** different lists that you could use to test two aspects of this program, and the expected output. [4]

   * **List 1:**
     * Input: A list of integers
     * Aspect Tested: VALID 
     * Expected Output: A list of integers that are divisible by 7

   * **List 2:**
     * Input: A list of strings
     * Aspect Tested: INVALID
     * Expected Output: Error

2. (a) Explain what is meant by **caching**, and give an example of when it might be used. [3]

   * **Definition:** sorting frequently used/ accessed instructions in a place faster to access, increasing overall speeds
   * **Example:** opening browser windows on a computer automatically on start-up 

   (b) Explain **one** benefit and **one** drawback of caching. [2]

   * **Benefit:** Faster - cache is faster to access than other locations
   * **Drawback:** Older versions that are cached may be shown over newer versions that are not cached

3. List **three** benefits of creating reusable program components when working on a large project. [3]

	1. Can be reused in different projects
	2. Fewer mistakes 
	3. Easier to maintain

***
[Total 20 marks]
#computer-science 